{
  "name": "streamer-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "convert": "node scripts/convert-video.mjs",
    "inspect": "node scripts/inspect-glb.mjs",
    "detect-zoom": "node scripts/detect-zoom.mjs",
    "compress-glb": "node scripts/compress-glb.mjs",
    "prepare-assets": "npm run convert && npm run compress-glb",
    "serve": "node scripts/server.mjs",
    "vercel-build": "echo 'static deploy — no build step'"
  },
  "dependencies": {
    "ffmpeg-static": "^5.2.0",
    "ffprobe-static": "^3.1.0"
  }
}
